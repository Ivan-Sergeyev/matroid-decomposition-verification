% templates for theorem environments in leanblueprint .tex source files

\begin{definition}[]
  \label{def:}
  \uses{}
  \todo[inline]{add name, label, uses, text}
\end{definition}

  \begin{proposition}[]
    \label{prop:}
    \uses{}
    \todo[inline]{add name, label, uses, text}
  \end{proposition}

\begin{lemma}[]
  \label{lem:}
  \uses{}
  \todo[inline]{add name, label, uses, text}
\end{lemma}

\begin{theorem}[]
  \label{thm:}
  \uses{}
  \todo[inline]{add name, label, uses, text}
\end{theorem}

\begin{corollary}[]
  \label{cor:}
  \uses{}
  \todo[inline]{add name, label, uses, text}
\end{corollary}

\begin{proof}[Proof sketch]
  \uses{}
  \todo[inline]{add uses, text}
  \begin{itemize}
    \item
  \end{itemize}
\end{proof}

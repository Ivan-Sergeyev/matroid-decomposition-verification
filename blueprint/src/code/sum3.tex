\begin{definition}[$3$-sum of matrices]
  \label{Matrix_3sumComposition}
  \lean{Matrix_3sumComposition}
  % \uses{}
  \leanok
  \todo[inline]{add}
\end{definition}

\begin{definition}[matroid $3$-sum]
  \label{BinaryMatroid.Is3sumOf}
  \lean{BinaryMatroid.Is3sumOf}
  \uses{BinaryMatroid,Matrix_3sumComposition}
  \leanok
  \todo[inline]{add}
\end{definition}

% todo: same as for 1-separations and 1-sums
% \begin{lemma}[$3$-separations and $3$-sums]
%   \label{lem:code_3_sep_3_sum}
%   \uses{BinaryMatroid.Is3sumOf,def:code_k_conn,def:code_k_sep}
%   \todo[inline]{add}
% \end{lemma}

\begin{theorem}[$3$-sum of regular matroids is regular]
  \label{BinaryMatroid.Is3sumOf.isRegular}
  \lean{BinaryMatroid.Is3sumOf.isRegular}
  \uses{BinaryMatroid.Is3sumOf,BinaryMatroid.IsRegular}
  \leanok
  \todo[inline]{add}
\end{theorem}

\begin{lemma}[left summand of regular $3$-sum is regular]
  \label{BinaryMatroid.Is3sumOf.isRegular_left}
  \lean{BinaryMatroid.Is3sumOf.isRegular_left}
  \uses{BinaryMatroid.Is3sumOf,BinaryMatroid.IsRegular}
  \leanok
  \todo[inline]{add}
\end{lemma}

\begin{lemma}[right summand of regular $3$-sum is regular]
  \label{BinaryMatroid.Is3sumOf.isRegular_right}
  \lean{BinaryMatroid.Is3sumOf.isRegular_right}
  \uses{BinaryMatroid.Is3sumOf,BinaryMatroid.IsRegular}
  \leanok
  \todo[inline]{add}
\end{lemma}

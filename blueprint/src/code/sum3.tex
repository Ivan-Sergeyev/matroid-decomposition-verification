\begin{definition}[$3$-sum of matrices]
  \label{Matrix_3sumComposition}
  \lean{Matrix_3sumComposition}
  % \uses{}
  \leanok
  \todo[inline]{add}
\end{definition}

\begin{definition}[matroid $3$-sum]
  \label{StandardRepresentation.Is3sumOf}
  \lean{StandardRepresentation.Is3sumOf}
  \uses{StandardRepresentation,Matrix_3sumComposition}
  \leanok
  \todo[inline]{add}
\end{definition}

% todo: same as for 1-separations and 1-sums
% \begin{lemma}[$3$-separations and $3$-sums]
%   \label{lem:code_3_sep_3_sum}
%   \uses{StandardRepresentation.Is3sumOf,def:code_k_conn,def:code_k_sep}
%   \todo[inline]{add}
% \end{lemma}

\begin{theorem}[$3$-sum of regular matroids is regular]
  \label{StandardRepresentation.Is3sumOf.isRegular}
  \lean{StandardRepresentation.Is3sumOf.isRegular}
  \uses{StandardRepresentation.Is3sumOf,StandardRepresentation.IsRegular}
  \leanok
  \todo[inline]{add}
\end{theorem}

\begin{lemma}[left summand of regular $3$-sum is regular]
  \label{StandardRepresentation.Is3sumOf.isRegular_left}
  \lean{StandardRepresentation.Is3sumOf.isRegular_left}
  \uses{StandardRepresentation.Is3sumOf,StandardRepresentation.IsRegular}
  \leanok
  \todo[inline]{add}
\end{lemma}

\begin{lemma}[right summand of regular $3$-sum is regular]
  \label{StandardRepresentation.Is3sumOf.isRegular_right}
  \lean{StandardRepresentation.Is3sumOf.isRegular_right}
  \uses{StandardRepresentation.Is3sumOf,StandardRepresentation.IsRegular}
  \leanok
  \todo[inline]{add}
\end{lemma}
